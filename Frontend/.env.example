# Pet Nexus Environment Variables
# Real mode is the default. Copy this file to .env only if you need to change it.
# Set VITE_USE_MOCK_DATA=true to work on the UI with no backend running.
VITE_USE_MOCK_DATA=false
VITE_API_BASE_URL=/api
