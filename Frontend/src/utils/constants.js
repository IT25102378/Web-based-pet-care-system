// =============================================================================
// Shared constants.
//
// Values that more than one screen needs, defined here once so they cannot
// drift apart. The frontend is the specification, so this file is the
// authoritative copy; the backend keeps a matching one in
// com.petnexus.backend.constants.AppConstants.
// =============================================================================

/**
 * Shown in place of a profile picture when a user has not uploaded one.
 * The file lives at Frontend/public/avatars/default-avatar.svg.
 */
export const DEFAULT_AVATAR_URL = '/avatars/default-avatar.svg';
